# Shiny Server configuration for ShinyPopGen Docker container

run_as shiny;

server {
  listen 3838;

  location / {
    app_dir /usr/local/lib/R/site-library/shinypopgen/app;
    log_dir /var/log/shiny-server;
  }
}
