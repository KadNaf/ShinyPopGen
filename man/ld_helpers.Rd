% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_helpers.R
\name{ld_helpers}
\alias{ld_helpers}
\title{Linkage disequilibrium (LD) helpers: contingency tables, G-test, permutations}
\description{
This block implements a simple LD test per population by:
\enumerate{
\item building haplotype contingency tables for all locus pairs,
\item computing a likelihood-ratio (G) statistic for each table,
\item estimating p-values by permutation (randomisation) within population.
}
}
\details{
The code assumes:
\itemize{
\item \code{data$Population} defines populations (one value per sample/row),
\item loci columns contain haplotype/genotype strings (e.g. "92/100"),
\item missing genotypes are coded as "0/0" when \code{include_missing = FALSE}.
}

Main functions
\itemize{
\item \code{create_contingency_tables(data, loci, include_missing)}:
For each population and each pair of loci, returns a contingency table
(rows = locus1 haplotypes, cols = locus2 haplotypes). Optionally drops
rows where either locus is "0/0".
\item \code{calculate_g_stat(contingency_table)}:
Computes expected counts under independence and the G statistic:
G = 2 * sum(O * log(O/E))   (only for cells with O > 0)
Returns both \code{expected} and \code{g_stat}.
\item \code{add_g_stats(contingency_tables)}:
Wraps each (pop, pair) table into a list containing:
observed contingency table, expected table, and G statistic.
Pair names are sanitised to use "." separators.
\item \code{randomized_g_stats(data, loci, n_simulations, calculate_g_stat, include_missing)}:
Permutation test per population and locus pair:
independently shuffles locus1 and locus2 columns within population,
recomputes G, and stores the simulated G distribution.
Parallelised over populations (foreach/doParallel).
}

P-values and reporting
\itemize{
\item \code{calculate_pvalues(observed_g_stats, simulated_g_stats, epsilon)}:
For each (pop, pair), estimates p = mean(G_sim >= G_obs - epsilon),
dropping NA simulations.
\item \code{calculate_global_pvalues(observed_g_stats, simulated_g_stats)}:
Computes a crude “global” p-value per locus pair by comparing the mean
observed G across populations to the pooled simulated G values.
\item \code{create_summary_table(pvalues, global_pvalues)}:
Builds a data.frame with one row per locus pair, one column per population
(p-values), plus a \code{Global_P_Value} column.
}
}
